# Copy to `.env` next to docker-compose.yml (optional overrides).
# Defaults in docker-compose.yml work for a first office install.

HOST_PORT=8787
OPERATOR_CODE=nhih-ops

POSTGRES_USER=nhih
POSTGRES_PASSWORD=nhih_ops_change_me
POSTGRES_DB=nhih_ops
